pipeline {
    agent {
        label 'curl'
    }

    stages {
        stage('Maven Clean') {
            steps {
                echo "do maven clean"
            }
        }

        stage('Build Docker Image') {
            steps {
                echo "building docker image"
            }
        }

        stage('Push Docker Image') {
            steps {
                echo "pushing docker image"
            }
        }
    }

    post {
        always {
            echo "doing clean up"
        }
    }
}
